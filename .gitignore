# dependencies
/node_modules
/.pnp
.pnp.*

# testing
/coverage

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
# …but the template has no secrets in it and every new machine needs it.
!.env.example

# typescript
*.tsbuildinfo

# AI tooling & local agent config
/.claude/
/.codex/
/.cursor/
/.impeccable/
/.agents/
/.opencode/
/skills/
CLAUDE.md
AGENTS.md

# Supabase CLI scratch state — project ref and local versions, not source.
/supabase/.temp/

# Finished working notes — kept locally, not versioned.
/completed md/
run_build.md

# Sample import/attendance CSVs — real names and addresses, kept locally.
/test_csv/
